import 'package:flutter/material.dart';

class DueBalance extends StatelessWidget {
  const DueBalance({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Due Balance"),
    );
  }
}
